export default function Enrolments() {
  return (
    <div className="space-y-6 max-w-5xl">
      <div>
        <h2 className="page-title">Enrolments</h2>
        <p className="text-sm text-slate-500 mt-1">Per-enrolment timeline + sentiment.</p>
      </div>
      <div className="card card-pad text-center py-12">
        <p className="text-sm font-medium text-slate-700">Coming soon</p>
        <p className="text-xs text-slate-500 mt-1">
          Per-lead enrolment timelines live on the <span className="font-medium">Leads → timeline</span> view for now.
        </p>
      </div>
    </div>
  );
}
